<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>TinyApp</title>
</head>
<body>
  <ul>
  <% for(let url in urls) { %>
    <li><%= urls[url] %> -> <%= url %></li>
  <%}%>
</ul>
<a href="#">Shorten URL</a>
</body>
</html>