<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>TinyApp</title>
</head>
<body>
  <% include ./partials/_header %>
  <ul>
  <% for(let url in urls) { %>
    <li>
      <form method="POST" action="/urls/<%= url %>/delete"><%= urls[url].longURL %> -> <%= url %>
        <a href="/urls/<%= url %>">Edit</a>
        <input type="submit" value="delete">
      </form>
    </li>

  <%}%>
</ul>
<a href="/urls/new">Shorten URL</a>
</body>
</html>